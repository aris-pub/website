---
layout: base.njk
title: Blog
description: Thoughts on the future of scientific publishing and knowledge infrastructure
---

<section class="blog-hero">
    <div class="container">
        <h1 class="section-title">Blog</h1>
        <p class="section-subtitle">Thoughts on the future of scientific publishing</p>
    </div>
</section>

<section class="blog-posts">
    <div class="blog-container">
        <div class="post-list">
            {% set posts = collections.post | reverse %}
            {% for post in posts %}
            <article class="post-preview">
                <h2 class="post-title">
                    <a href="{{ post.url }}">{{ post.data.title }}</a>
                </h2>
                <p class="post-meta">{{ post.date | dateFormat }} • {{ post.data.author or 'Aris Team' }}</p>
                <div class="post-excerpt">
                    {{ post.data.description or (post.templateContent | excerpt) | safe }}
                </div>
                <a href="{{ post.url }}" class="read-more">Read more →</a>
            </article>
            {% endfor %}
            
            {% if posts.length == 0 %}
            <div class="post-preview">
                <h2 class="post-title">Coming Soon</h2>
                <p class="post-meta">Aris Team</p>
                <div class="post-excerpt">
                    We're preparing thoughtful content about the future of scientific publishing, research infrastructure, and open science. Check back soon for our first posts!
                </div>
            </div>
            {% endif %}
        </div>
    </div>
</section>